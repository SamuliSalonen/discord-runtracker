{
  "name": "discord-runtracker",
  "version": "1.0.0",
  "description": "simple discord bot to track kilometers run in a group",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SamuliSalonen",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^16.0.1"
  }
}
